<div class="flex gap-8 flex-col items-center justify-center mt-32">
  <header class="flex flex-col items-center">
    <%= image_tag image, class: 'w-80 h-80' %>
    <div class="flex flex-col gap-y-2 text-center max-w-3xl">
      <h1 class="text-2xl text-primary-700"><%= title %></h1>
      <p class="text-neutral-400 text-lg"><%= paragraph %></p>
    </div>
  </header>

  <div class="buttons-group justify-center">
    <%= content %>
  </div>
</div>
